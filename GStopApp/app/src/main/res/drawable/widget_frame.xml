<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing, drawn behind the widget. It exists so the debug source set can replace it with the
    dev build's orange ring without the layout, or either enneagram drawable, having to be
    duplicated per build type. A release widget draws this and sees no difference.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
</shape>
