<?xml version="1.0" encoding="utf-8"?>
<!--
    The icon's ground. Plain black, as it has always been — a drawable rather than a bare colour
    only so the debug source set has something of its own to override, which is where the dev
    build's orange ring comes from. Nothing here changes what a release icon looks like.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path
        android:fillColor="#000000"
        android:pathData="M0,0 L108,0 L108,108 L0,108 Z" />
</vector>
